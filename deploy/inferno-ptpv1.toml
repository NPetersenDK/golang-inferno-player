# Statime PTPv1 configuration for Dante synchronization
loglevel = "info"
domain = 0
slave-only = true
virtual-system-clock-base = "monotonic"
usrvclock-export = true
usrvclock-path = "/tmp/usrvclock"

[[port]]
interface = "eth0"
